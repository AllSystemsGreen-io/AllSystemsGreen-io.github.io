# AllSystemsGreen.io Agent Policy

Before staging, committing, pushing, deploying, deleting, moving, or deciding
where project files belong, read the central source registry:

`C:\Users\Max\Documents\TelemetryBase\docs\project-source-registry.json`

If that registry is missing, ambiguous, stale, incomplete, or conflicts with
the current branch, upstream, remotes, or dirty files, alert Max before making
git or placement changes.

Treat this repo as the public AllSystemsGreen.io site source unless the
registry says otherwise.
